# Public Expo / Supabase client (RLS-gated anon key only)
# Shared project Rank AI / Mediplan / Lobby
EXPO_PUBLIC_SUPABASE_URL=https://mjzjracjadlybvdttgto.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Member PWA origin (QR / join HTTPS links). Empty → window.location.origin at runtime.
EXPO_PUBLIC_WEB_ORIGIN=https://lobby-app.pages.dev

# Set to 1 to force in-app demo data (no live Supabase).
# EXPO_PUBLIC_DEMO=1

# NEVER put SUPABASE_SERVICE_ROLE_KEY in the mobile app.
# Privileged ops: issue-seal, compute-matches, send-signal → Edge Functions only.
