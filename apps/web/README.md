# Deprecated path

The Next.js backoffice lives at **`apps/backoffice`** (Cloudflare Workers deploy).

Do not use `apps/web` for new work. Env templates:

- Prefer: `apps/backoffice/.env.example`
- This folder’s `.env.example` is a stub pointing to the same variables for older docs/scripts.
